name: GitHub Spellcheck Action 

on: [push]

jobs:
  spell-check:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Spellcheck Action 
        uses: rojopolis/spellcheck-github-actions@0.4.0
